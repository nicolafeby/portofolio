services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: nicola-portfolio:local
    container_name: nicola-portfolio
    ports:
      - "${PORT:-1998}:80"
    restart: unless-stopped
